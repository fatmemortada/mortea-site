// Mortéa Phase 41 — Provider Approval Workflow

function approveProvider(providerId) {
  console.log("Provider approved:", providerId);
}

function rejectProvider(providerId) {
  console.log("Provider rejected:", providerId);
}
