// Mortéa Phase 30 — Multi-Location Support

function createLocation(providerId, locationData) {
  console.log("Creating location:", providerId, locationData);
}

function listLocations(providerId) {
  return [];
}
