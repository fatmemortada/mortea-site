// Mortéa Phase 25 — Risk & Fraud Detection

function createRiskFlag(providerId, reason) {
  console.log("Risk flag created:", providerId, reason);
}

function detectSpamActivity(activity) {
  return activity.includes("spam");
}
