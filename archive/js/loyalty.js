// Mortéa Phase 23 — Loyalty System

function calculatePoints(amountSpent) {
  return Math.floor(amountSpent * 10);
}

function addLoyaltyPoints(userId, points) {
  console.log(`Adding ${points} points to user ${userId}`);
}
