// Mortéa Phase 33 — SEO Engine

function generateSEOTitle(providerName, city) {
  return `${providerName} | Luxury Beauty Services in ${city} | Mortéa`;
}

function generateMetaDescription(providerName, services) {
  return `${providerName} offers ${services.join(", ")} on Mortéa.`;
}\n